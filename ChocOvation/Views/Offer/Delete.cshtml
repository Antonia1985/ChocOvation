@model ChocOvation.Models.Offer

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Offer</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            Total Price/Quality
        </dt>

        <dd>
            @Html.DisplayFor(model => model.TotalPriceQuality)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.DateOfOffer)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.DateOfOffer)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        
        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            @{  var check = ViewBag.checkRole;

                if (check)
                {
                    @Html.ActionLink("Back to List", "MyOffers")
                }
                else
                {
                    @Html.ActionLink("Back to list", "Index", "Offer"@*, new { id = supplierid }, htmlAttributes: new { @class = "" }*@)
                }

            }
            
        </div>
    }
</div>
